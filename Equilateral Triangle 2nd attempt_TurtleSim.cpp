#include<simplecpp>

main_program{
  turtleSim();
  left(60); forward(200); wait(2);
  right(90); right(30);
  forward(200); wait(2);
  right(90); right(30);
  forward(200); wait(2);
  right(90); right(30);
}
